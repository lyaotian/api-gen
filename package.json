{
  "name": "api-gen",
  "version": "1.0.0",
  "description": "",
  "main": "bin/index.js",
  "scripts": {
    "build": "babel src -d bin -s",
    "watch": "babel src -d bin -s -w",
    "start": "npm run build && node bin/index.js",
    "test": "babel src -d bin -s"
  },
  "author": "Yaotian",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0"
  },
  "dependencies": {
    "download": "^5.0.2",
    "fs-extra": "^0.30.0",
    "mustache": "^2.2.1"
  }
}
